# タスク管理システム 設計書

最終更新: 2026-08-03
対象リポジトリ: `taskmanager`（AWS SAMモノレポ、スタック名 `taskmanager` / `task-tables`）
ステータス: **Client/Series/Frameモデルへの再設計に加え、履歴機能・Bedrock AgentCore連携エージェント機能を実装。2026-08-01にメインスタックを`task-mcp-server`から`taskmanager`へ移行・再デプロイし、履歴・エージェント機能を含む現行`template.yaml`一式が本番AWS環境で稼働確認済み**（10-3節参照）。**2026-08-02、外部リポジトリ`agentmanager/receipt-agent`を本リポジトリ`task-agent/`へコード移設（名称も`task-agent`に統一）。新Bedrock AgentCore Runtime（`task_agent-lAAoax7UkB`）としてデプロイし、実際のクライアント一覧照会（DynamoDB直接参照）で動作確認済み。`template.yaml`の`AgentRuntimeArn`デフォルト値も新ARNへ更新の上、taskmanagerスタック側も`sam deploy`済み**（4-4-1節参照）。旧`agentmanager/receipt-agent`リポジトリ・旧runtimeは本統合をもって役目を終えている（12章参照）。**2026-08-02〜03、履歴（History）機能を拡張: 履歴エントリにタスク（Series）・対象月（Frameの複数選択）を紐付け可能にし、その場でTaskへ進捗を反映する「進捗反映」ボタンを追加。あわせて編集UIを「インライン編集」から「行クリックで上部フォームへ読み込んで編集」方式へ刷新した**（4-5節参照）。**2026-08-03、履歴の`content`をキーワード/正規表現ルールで自動分類する「分類機能」を実装（4-7節）。新設の`TaskClassificationRules`テーブル・`layer/python/task_repository.py`の分類軸/ルールCRUD+`classify()`・`api_src/app.py`の`/classification-axes`系API+`/classify`API・フロントエンドの「分類ルール」ページ（`ClassificationAxesPage.jsx`）・`HistoryPanel.jsx`の「自動判定」ボタンまで実装。`task-tables`（`TaskClassificationRules`テーブル実体作成）・`taskmanager`（分類機能関連の環境変数・IAMポリシー・APIルート）の両スタックとも`aws cloudformation deploy`/`sam deploy`済みで、Web UI上で軸・ルールの作成と「自動判定」ボタンの動作を確認済み**（4-7節参照）。

> 本書はコードベース・インフラ定義（`template.yaml` / `tables.yaml` / `amplify.yml`）・README.md・コミット履歴を突き合わせて作成した設計書である。個人〜社内数名運用の小規模システムであるため、正式なフレームワークに則った過剰な設計ドキュメントは避け、**現状の実装がなぜその形になっているか**を中心にまとめる。

---

## 1. システム概要

- **用途**: 社内のタックス事務所（BPO: Business Process Outsourcing業務）向けのタスク管理ツール。
- **利用者**: 社内スタッフ数名（Web UI経由）、および担当者本人（Claude Desktop / claude.ai経由の自然言語操作）。
- **中核ドメイン概念**: 「クライアント（Client）」に対して、「シリーズ（Series、業務の種類）× フレーム（Frame、対象月）」の組み合わせで最小単位の「タスク（Task）」を管理する。例:「クライアントAの資料受領・2026年6月分」。加えて、クライアントごとの時系列の「履歴（History）」記録と、Google Driveフォルダを対象に外部AIエージェント（Bedrock AgentCore）を呼び出す「エージェント」機能を持つ。
- **最大の特徴**: **Web UIとClaude(MCP)という2つの利用インターフェースが、同一のDynamoDBデータソースを共有するデュアルインターフェース構成**。Web UIで更新した内容はClaude経由でも即座に参照でき、逆にClaude経由で作成・更新したタスクもWeb UI上でポーリング経由（4秒間隔）で確認できる。

### 開発の経緯（コミット履歴より、抜粋）

| コミット | 日付 | 内容 |
|---|---|---|
| a87041c | 2026-06-30 | 初回コミット。SAM + Lambda2種 + Reactフロントエンドの一式を一括構築 |
| e26b9e3 | 2026-07-27 | ドメインモデルをClient/Series/Frameに再設計 |
| d5c7c24 | 2026-07-27 | 設計書・READMEをClient/Series/Frameモデルに合わせて更新（本書の前バージョン） |
| d92c1cf / 6409d93 / ddb114f | 2026-07-28 | ハンバーガーメニューでクライアント/シリーズ/フレーム一覧を追加し編集可能に。シリーズに`taskGroup`（分類）属性を追加 |
| ac56b95 | 2026-07-28 | ステータスを3段階から6段階へ拡張、配色を分離 |
| c2b8095 | 2026-07-30 | `ClientSidebar.jsx`を廃止し`ClientSelector.jsx`（パネルヘッダー内ドロップダウン）に置き換え |
| 32dfa94 / 745c0d4 | 2026-07-30〜31 | 履歴タブ追加。TaskHistoryテーブル・関連API・リポジトリ関数を新設 |
| 390a3ea | 2026-07-31 | 履歴タブにCSVアップロード機能を追加 |
| 5c9803f | 2026-07-31 | MCPツールに履歴関連3関数を追加 |
| 6e90624 / b14bd1e / 47b4998 / d47a54b | 2026-08-01 | 「エージェント」タブ・非同期ジョブディスパッチLambda・Bedrock AgentCore連携を新設。Google DriveフォルダID関連の不具合修正 |
| ba3a545 | 2026-08-02 | 外部リポジトリ`agentmanager/receipt-agent`を本リポジトリ`task-agent/`へコード移設 |
| 6984571 | 2026-08-02 | `task-agent`をBedrock AgentCore Runtime（新ARN `task_agent-lAAoax7UkB`）として`agentcore launch`でデプロイ。`template.yaml`の`AgentRuntimeArn`デフォルト値を新ARNへ更新 |
| a12d7fb | 2026-08-02 | `task-agent完成`（実体はロックファイル削除のみ。taskmanagerスタック側`sam deploy`はこの前後の手動作業で完了） |
| 43a2aca / 65eaa3d | 2026-08-02 | 「エージェント」タブの`pinger`を`progress`（進捗更新エージェント）へ置き換え。AgentCore実行ロールに`TaskHistory`へのDynamoDB権限を追加（4-4-2節） |
| 416629a | 2026-08-03 | 履歴（History）エントリに`seriesCode`（タスク名）・`frameCodes`（対象月、複数選択）を追加。`FrameMultiSelect.jsx`を新設（4-5節） |
| b5152c9 / 74a709b / b345d0f / 0cd2304 / ccf77f2 | 2026-08-03 | 履歴タブの編集UXを「インライン編集」から「行クリックで上部フォームへ読み込んで編集」方式へ刷新。分類→タスク名の絞り込み、Task進捗へ反映する「進捗反映」ボタン、日付(MM/DD)・対象月(連続月の範囲表記)の表示整形を追加（4-5節） |

作者は一貫して単独（`woodygaragelab`）。CI/CDやテストは未整備という前提を踏まえて読むこと。

---

## 2. 全体構成図

```mermaid
flowchart TB
    subgraph ClientUsers["利用者"]
        WebUser["社内スタッフ<br/>(ブラウザ)"]
        ClaudeUser["担当者<br/>(Claude Desktop / claude.ai)"]
    end

    subgraph Frontend["フロントエンド (Amplify Hosting)"]
        SPA["React SPA<br/>(frontend/)"]
    end

    subgraph AuthN["認証"]
        Cognito["Cognito User Pool<br/>(Web UI用)"]
    end

    subgraph API["AWS SAM スタック taskmanager"]
        APIGW["API Gateway HTTP API<br/>+ CognitoAuth Authorizer"]
        WebApiLambda["TaskApiFunction<br/>(api_src/app.py)"]
        McpLambda["TaskMcpFunction<br/>(mcp_src/app.py, Function URL)<br/>+ AccessControlMiddleware"]
        AgentJobLambda["TaskAgentJobProcessorFunction<br/>(agent_job_src/app.py)<br/>Timeout 900秒、Event非同期起動"]
        Layer["TaskRepositoryLayer<br/>(layer/python/task_repository.py)"]
    end

    subgraph TaskAgent["task-agent/ (本リポジトリ内、Docker+agentcore CLIで別デプロイ経路)"]
        AgentCore["Bedrock AgentCore Runtime<br/>task_agent.py<br/>(領収書分類「archivist」エージェント)"]
    end

    subgraph External["外部サービス"]
        Drive["Google Drive<br/>(receipt / renamed フォルダ)"]
    end

    subgraph Data["DynamoDBスタック task-tables"]
        Tasks[("Tasks<br/>PK=clientCode SK=taskKey")]
        Clients[("TaskClients")]
        Series[("TaskSeries")]
        Frames[("TaskFrames")]
        History[("TaskHistory<br/>PK=clientCode SK=historyId")]
        AgentJobs[("TaskAgentJobs<br/>PK=clientCode SK=jobId, TTL=1日")]
    end

    WebUser -->|ログイン| SPA
    SPA -->|IDトークン取得| Cognito
    SPA -->|Bearer IDToken| APIGW
    APIGW -->|CognitoAuth検証| Cognito
    APIGW --> WebApiLambda

    ClaudeUser -->|Bearerトークン<br/>or AnthropicIP| McpLambda

    WebApiLambda --> Layer
    WebApiLambda -->|Event非同期invoke| AgentJobLambda
    McpLambda --> Layer
    AgentJobLambda --> Layer
    AgentJobLambda -->|invoke_agent_runtime| AgentCore
    AgentCore -->|ファイルリネーム・分類| Drive
    AgentCore -.->|task_repository(ビルド時コピー)経由でQuery| Clients
    Layer --> Tasks
    Layer --> Clients
    Layer --> Series
    Layer --> Frames
    Layer --> History
    Layer --> AgentJobs
```

- インフラはCloudFormation/SAMで**2スタックに分離**されている: `task-tables`（DynamoDB、`tables.yaml`、独立ライフサイクル）と`taskmanager`（Lambda3種・Cognito・API Gateway、`template.yaml`）。テーブルを消さずにアプリ側だけ再デプロイできるようにする意図。
- 3つのLambda（Web API用・MCP用・エージェントジョブ処理用）は共通のLambda Layer（`task_repository`）を参照し、ビジネスロジックの二重実装を避けている。
- `TaskAgentJobProcessorFunction`のみ、Bedrock AgentCore（外部のAIエージェント実行基盤）を呼び出す唯一のコンポーネント。DynamoDBの6テーブルに加え、この処理系だけがGoogle Drive上のファイル操作を伴う副作用を外部（Bedrock AgentCore Runtime側）に持つ点で他の2Lambdaと性質が異なる（4-4章参照）。
- `task-agent/`（旧`agentmanager/receipt-agent`、2026-08-02に本リポジトリへ移設・再デプロイ済み）はSAM/Lambdaではなく、Docker + `agentcore` CLIで別途デプロイされるBedrock AgentCore Runtimeアプリである。`template.yaml`のビルド・デプロイ対象には含まれない。案件マスタ（`TaskClients`）の参照は、`layer/python/task_repository.py`をビルド時に`task-agent/`へコピーして直接importする方式（詳細は4-4-1節）。

---

## 3. 技術スタック

| レイヤ | 技術 |
|---|---|
| バックエンド言語 | Python 3.13 |
| MCPサーバー | `mcp`（公式SDK, `FastMCP`）+ `mangum`（ASGI→Lambdaアダプタ）+ `boto3` |
| Web API | フレームワーク不使用（API Gateway HTTP APIイベントを素朴にif分岐でルーティング）+ `boto3`（Lambda標準ランタイム同梱） |
| 非同期エージェントディスパッチ | `boto3`の`bedrock-agentcore`サービスクライアント（追加の依存パッケージなし、ランタイム同梱boto3のサービスモデルに依存） |
| 外部AIエージェント実行基盤 | Amazon Bedrock AgentCore Runtime（本リポジトリ外で管理される別スタック。ARNのみ`template.yaml`のパラメータとして参照） |
| データストア | Amazon DynamoDB（`PAY_PER_REQUEST`、ORM・マイグレーションツール不使用、GSIなし） |
| 認証 | Amazon Cognito User Pool（Web UI）／独自Bearerトークン+IPホワイトリスト（MCP） |
| インフラIaC | AWS SAM（`AWS::Serverless-2016-10-31`） |
| フロントエンド | React 18.3 + Vite 5.4 |
| フロントエンド認証UI | `@aws-amplify/ui-react` 6.5 + `aws-amplify` 6.6（Authenticator、日本語UIラベルにカスタマイズ） |
| 状態管理 | なし（React標準の`useState`/`useEffect`のみ、Redux等未使用） |
| ルーティング | なし（React Router未使用。ハンバーガーメニューによる`currentView`状態切り替えと、パネル内`activeTab`状態切り替えの2階層のstate駆動画面遷移） |
| CSV処理 | 外部ライブラリ不使用。`HistoryPanel.jsx`内に自前実装のパーサ（引用符・エスケープ・改行コード対応） |
| ホスティング | AWS Amplify Hosting（GitHub連携、pushで自動ビルド） |
| CI/CD | なし（Amplify Hostingの自動ビルドのみ、テスト実行やLintのパイプラインは未構築） |
| テスト | なし（テストコード・フレームワークともに未導入。動作確認はLambda直接invokeによる手動スモークテスト、および開発時のヘッドレスChromeスクリーンショットによる目視確認） |

---

## 4. ドメインモデル

### 4-1. 中核エンティティ

| エンティティ | 属性 | 説明 |
|---|---|---|
| **Client**（クライアント） | `clientCode`, `clientName`, `receiptFolderId`（任意）, `renamedFolderId`（任意） | 担当先。`receiptFolderId`/`renamedFolderId`はGoogle DriveフォルダID（4-4節参照） |
| **Series**（シリーズ、UI表示名「タスク」） | `seriesCode`, `seriesName`, `taskGroup`（表示名「分類」） | 業務の種類。例:「資料受領」「資料整理」「データ入力」。クライアント横断の共通マスタ。`taskGroup`は進捗表・タスク表の左端の分類列に使う自由記述の集約キー |
| **Frame**（フレーム、UI表示名「月」） | `frameCode`（`YYYYMM`形式、例`202606`）, `frameName` | タスクの対象タイムフレーム。例:「2026年6月」。年ごとに別レコード |
| **Task**（タスク） | `clientCode`+`seriesCode`+`frameCode`（複合キー）, `status`, `assignee`, `completeDate` | 最小単位の作業。あるクライアントの、あるシリーズの、あるフレーム分の進捗を1レコードで表す |
| **History**（履歴） | `clientCode`+`historyId`（UUID）, `date`, `category`, `seriesCode`（任意）, `frameCodes`（任意、配列）, `assignee`, `status`, `content` | クライアントごとの時系列の作業記録。Task（Series×Frameの定型業務）とは独立した自由記述のログだが、2026-08-03以降は`seriesCode`/`frameCodes`で特定のTaskと紐付け、そこから進捗を反映することもできる。4-5節参照 |
| **AgentJob**（エージェントジョブ） | `clientCode`+`jobId`（UUID）, `agentId`, `status`, `prompt`, `result`/`error`, `ttl` | 「エージェント」タブから発行する非同期処理の実行記録。4-4節参照 |

**Client × Series × Frame の関係**（変更なし）:
- Series・Frameはクライアント横断の共通マスタ（クライアントごとの個別定義はしない）。
- Taskが`clientCode+seriesCode+frameCode`を持つことで「どのクライアントの・どの業務の・どの月分か」を一意に表す。
- Series・Frameは**事前登録不要**。タスク作成時に未登録のコードを指定すると自動的にマスタへ追加される（「使ったら生成される」設計）。加えて2026-07-28以降、ハンバーガーメニュー配下の一覧画面（`ClientListPage`/`SeriesListPage`/`FrameListPage`）から明示的な新規登録・削除も可能になった（4-3節参照）。
- Clientのみ、タスク作成に先立ちドロップダウンからの明示的な新規登録操作（`create_client`/`POST /clients`）を持つ。理由は9-2参照。

### 4-2. ステータス

```
未着手 → 依頼中 → 確認中 → 進行中 → 完了 / -
```
2026-07-28（`ac56b95`）に3段階から**6段階**へ拡張された: `未着手` / `依頼中` / `確認中` / `進行中` / `完了` / `-`（2026-08-02に`NA`から`-`へ表記変更、既存データも一括置換済み）。ドロップダウンで任意の値へ自由に行き来できる（一方向固定ではない）。UI上は「印影（はんこ）」風の`.stamp`クラスで、各段階に個別の配色（`frontend/src/App.css`）を割り当てている。`完了`のみ他の5段階と異なり傾き（`rotate`）を付けず、まっすぐ押されたスタンプとして視覚的に区別している。`completeDate`は`status`とは独立した手動入力項目（変更なし）。

### 4-3. マスタの編集可能化（2026-07-28）

`d92c1cf`（ハンバーガーメニュー追加）と`6409d93`（編集可能化）により、Client/Series/Frameの3マスタはいずれもWeb UI上の専用一覧画面から新規作成・削除ができるようになった。従来「タスク作成フローの中でのみ選択される付随的な存在」だったSeries/Frameも、この変更以降は独立した管理画面を持つ（ただし「タスク作成時に未登録なら自動登録」という4-1のフォールバック挙動自体は残っている）。

- `create_series`/`delete_series`、`create_frame`/`delete_frame`という明示的なCRUD関数が`task_repository.py`に追加された。これらは`create_task`内部の`_ensure_series_exists`/`_ensure_frame_exists`（冪等・サイレント登録）とは別物で、既存コードを指定すると`SeriesAlreadyExistsError`/`FrameAlreadyExistsError`を送出する（9-2節の非対称設計は維持しつつ、Series/Frame側にも明示的な登録手段が追加された形）。
- `update_client`/`delete_client`も同時に追加され、Clientマスタも一覧画面から編集・削除できる。

### 4-4. エージェント機能（2026-08-01、`6e90624`〜`d47a54b`）

Web UIの「エージェント」タブから、Amazon Bedrock AgentCore上で動く外部のAIエージェントを呼び出せる機能。API Gatewayの30秒制限を超える処理時間を扱うため、専用の非同期ジョブディスパッチLambda（`TaskAgentJobProcessorFunction`）を新設した。

**エージェントカタログ**: `frontend/src/AgentsPanel.jsx`に5種類のエージェントがハードコードされているが、現時点で実装（`LIVE_AGENT_IDS`）されているのは`archivist`（分類）・`progress`（進捗更新、4-4-2節参照）の2つのみ。残り3種（`scout`＝受領/情報収集、`courier`＝会計/外部連携、`auditor`＝チェック/検証）はUI上のプレースホルダで、「開始する」ボタンは無効化されている（旧`pinger`＝疎通確認/監視は2026-08-02に`progress`へ置き換えられ削除済み）。

**`archivist`（分類）エージェントの役割**: クライアントのGoogle Drive上の`receiptFolderId`フォルダにある未整理の領収書画像を、勘定科目ごとにリネーム・分類する。出力は`renamedFolderId`フォルダを想定。

**ジョブのライフサイクルとアーキテクチャ**:
1. ユーザーが「エージェント」タブでエージェントを選択し「開始する」を押すと、フロントエンドは`POST /clients/{clientCode}/agent-jobs`（body: `agentId`, `prompt`）を呼ぶ。`prompt`は`buildArchivistPrompt(client)`が組み立てる文字列で、`client.receiptFolderId`が設定されていれば対象フォルダのDrive URLをプロンプト本文に埋め込む（`d47a54b`で、表示中の指示内容と実際に送信されるプロンプトが食い違っていた不具合を修正し、両者を同一のヘルパー関数に統一した）。
2. `TaskApiFunction`（`api_src/app.py`）はこのリクエストを受けると、`task_repository.create_agent_job()`で`TaskAgentJobs`テーブルに`status="processing"`のジョブレコードを同期的に作成し、202を返すと同時に`TaskAgentJobProcessorFunction`を`InvocationType="Event"`（非同期・fire-and-forget）で起動する。API Gatewayの背後にいないこの後続Lambdaは`Timeout: 900`秒まで許容できる。
3. `TaskAgentJobProcessorFunction`（`agent_job_src/app.py`）は起動されると、まず`claim_agent_job()`で`ConditionExpression="attribute_not_exists(claimedAt)"`による条件付き更新を試みる。これは**Lambdaの非同期呼び出しがエラー・タイムアウト時に既定で自動リトライする**ため、対策なしでは同一ジョブに対してBedrock AgentCore呼び出し（および実際のDrive操作という副作用）が重複してしまうことへの防御。claimに失敗（＝既に処理済み/処理中）した場合は即座にreturnする。
4. claimに成功したら`boto3.client("bedrock-agentcore").invoke_agent_runtime()`を呼ぶ。`runtimeSessionId`にはジョブIDをそのまま使い、DynamoDB側のジョブレコードとAgentCore側のセッションを1:1に対応付けている。タイムアウトは`connect_timeout=10, read_timeout=850, retries={"max_attempts": 1}`という値を明示的に設定している。理由は、botocoreの既定`read_timeout`(60秒)は実際のAgentCore実行時間より短く、これを超えるとサーバー側はまだ実行中にもかかわらずboto3がクライアント側のソケットタイムアウトを起点に同一`runtimeSessionId`へ再送してしまい、Drive側の二重処理につながりかねないため。
5. 実行結果（またはエラー）を`complete_agent_job()`/`fail_agent_job()`で`TaskAgentJobs`に書き戻す（ステータスは`completed`/`error`）。`TaskAgentJobProcessorFunction`自身は`Tasks`/`TaskHistory`テーブルには一切書き込まない — Drive上のファイル操作を含む実際の副作用はすべてBedrock AgentCore Runtime側（このリポジトリの外）で完結する。
6. フロントエンド（`AgentsPanel.jsx`）は`GET /clients/{clientCode}/agent-jobs/{jobId}`を3秒間隔・最大100回（約5分）ポーリングし、`status`が`processing`でなくなった時点で`completed`→`done`/それ以外→`error`としてチケットのUIを更新する。タイムアウト・ポーリング自体のエラーもUI上で`error`表示する。チケットの表示状態は`useState`のみで保持する純粋なクライアント側状態であり、ページを再読み込みすると失われる（ただし裏側の`TaskAgentJobs`レコード自体はTTLの1日間は残る）。
7. `TaskAgentJobs`テーブルは`TimeToLiveSpecification`（属性`ttl`、`AGENT_JOB_TTL_SECONDS = 60*60*24`＝1日）でDynamoDB側から自動削除される。

**IAM上の注意点（`d47a54b`で修正済み）**: `bedrock-agentcore:InvokeAgentRuntime`の実際の認可チェックはベースのランタイムARNではなく`.../runtime-endpoint/*`配下のARNに対して行われる。当初ベースARNのみを許可していたため`AccessDeniedException`が発生し、`template.yaml`のIAMポリシーに`${AgentRuntimeArn}/runtime-endpoint/*`を追加して解消した。

**MCPサーバー（Claude側）からはエージェント機能は一切利用できない**（`create_agent_job`等はMCPツールとして登録されていない）。あくまでWeb UI専用の機能。

### 4-4-1. task-agent（旧`agentmanager/receipt-agent`、2026-08-02移設・再デプロイ完了）

`archivist`エージェントの実体は`task-agent/`配下のBedrock AgentCore Runtimeアプリ（`task_agent.py`、エントリポイント）。以前は`agentmanager`という別リポジトリで`receipt-agent`という名前で管理されており、`AgentRuntimeArn`パラメータが指すARN経由でのみtaskmanagerと接続していた。2026-08-02に本リポジトリの`task-agent/`へ移設し、名称も`task-agent`に統一。同日中に新しいBedrock AgentCore Runtime（`agent_id: task_agent-lAAoax7UkB`、ARN `arn:aws:bedrock-agentcore:ap-northeast-1:155830630328:runtime/task_agent-lAAoax7UkB`）として`agentcore launch`でデプロイし、`template.yaml`の`AgentRuntimeArn`パラメータのデフォルト値を旧ARN（`agent_agentcore-OTeMr39nYA`）から新ARNへ更新した上で、taskmanagerスタック側も`sam deploy`済み（旧`agentmanager/receipt-agent`側のruntimeは本統合をもって用済みとなった）。

**内部実装**（移設に伴い初めてtaskmanagerリポジトリから見えるようになった詳細）: `task_agent.py`は`BedrockAgentCoreApp`のentrypoint内でClaude Agent SDK（`claude_agent_sdk.query`）を呼び出し、Claude自身をエージェントとして動かす構成。インプロセスのSDK MCPサーバーを2つ内蔵する:
- **`google-drive`サーバー**: `search_files`/`get_file_metadata`/`download_file_content`/`create_file`/`trash_file`の5ツール。Google Drive認証は**サービスアカウントではなくユーザーのOAuthリフレッシュトークン**（Secrets Manager `task-agent/google-drive-user-token`、`get_refresh_token.py`で発行）を使う。サービスアカウントには保存容量（storage quota）が割り当てられずアップロードに失敗するため、この方式に変更された経緯がコード内コメントに残る。
- **`task-manager`サーバー**: `list_clients`の1ツールのみ。`layer/python/task_repository.py`をビルド時にコピーして直接import し、`repo.list_clients()`でDynamoDBへ直接Queryする（旧方式との対比は下記）。

ツール名（`mcp__google-drive__*` / `mcp__task-manager__list_clients`）は`agentmanager/receipt-agent`時代から変更していないため、`task-agent/.claude/skills/receipt-ocr-filelist/SKILL.md`は移設に伴う変更が不要だった。`build_agent_options()`の`system_prompt`は現状「Ikkoh K.K.（クライアントコード`IKK`）のBPO業務を支援する」という固定文言で、クライアント横断ではなくIKK向けに特化した内容になっている点に注意（複数クライアント対応は今後の検討事項、12章参照）。

**案件マスタ（`TaskClients`）参照方式の変更**:
- **旧方式**: `receipt-agent`が起動時にSecrets Manager（`receipt-agent/task-mcp-server`）からURL/Bearerトークンを取得し、taskmanagerの`taskmanager-mcp`（Function URL）へHTTPでMCP接続して`list_clients`を呼んでいた。
- **新方式**: `task-agent`が同一リポジトリ内の`layer/python/task_repository.py`をビルド時に取り込んで直接import し、`repo.list_clients()`をDynamoDBへ直接Queryする。

この結果、`task-agent`はもうtaskmanagerの`taskmanager-mcp`（Function URL）にHTTPで依存しない。ただし依然として`template.yaml`（SAM）のビルド・デプロイ対象ではなく、`Dockerfile`+`agentcore` CLIという別経路でデプロイされる点は変わらない（`task_repository.py`はDockerビルド時に`layer/python/`からコピーする運用、`task-agent/.gitignore`対象）。

**IAM**: AgentCore Runtimeの実行ロール（`AmazonBedrockAgentCoreSDKRuntime-ap-northeast-1-b93c04ec99`、`.bedrock_agentcore.yaml`の`execution_role_auto_create: false`によりtoolkitでは自動作成されず事前に手動用意する運用）に対し、`task-agent/dynamodb-access-policy-clients.json`（`TaskClients`への`dynamodb:Query`）・`task-agent/secrets-access-policy-usertoken.json`（`task-agent/google-drive-user-token-*`への`secretsmanager:GetSecretValue`）の2ポリシーをアタッチする必要がある。両ポリシーはリポジトリにJSONファイルとして同梱されているのみで、`template.yaml`等のIaCでは管理されていない（アタッチ自体はAWSコンソール/CLIでの手動作業）。

**動作確認**: `task-agent/payload.json`（送信プロンプト）・`task-agent/output.json`（実行結果）に、デプロイ後の実クライアントへの問い合わせ（「IKKの登録クライアント一覧を教えて」）に対し、DynamoDB上の実データ（IKK/LL/MM、各`receiptFolderId`/`renamedFolderId`含む）を正しく返した記録が残っている。新runtimeのDynamoDB直接参照・Secrets Manager経由のDrive認証がいずれも本番AWS環境で機能していることの実証となっている。

### 4-4-2. progress-update エージェント（2026-08-02追加）

エージェントタブの`pinger`（疎通確認、ダミー表示）を`progress`（進捗更新）に置き換え、`archivist`と同じくAgentCoreに実接続する2つ目のライブエージェントとした。フロントエンド側のプロンプト組み立ては`AgentsPanel.jsx`の`PROMPT_BUILDERS`マップで`agent.id`ごとに切り替える方式へ一般化した（従来は`archivist`用の`buildArchivistPrompt`が全エージェント共通でハードコードされており、2つ目のライブエージェントを追加すると誤動作する設計だった）。`progress`用のプロンプトは`buildProgressPrompt(client)`が`"{clientCode}の進捗を更新して"`を組み立てる。入力/出力フォルダのDriveリンク表示（4-4節）は`archivist`固有のUIのため、`agent.id === "archivist"`のときだけ表示するよう条件分岐した。

**役割**: 4-5節のCSVアップロードで一括登録された履歴（`date`/`category`/`assignee`/`status`が空、`content`のみ埋まっている状態）を対象に、`content`の文面をLLMが解釈して4項目を判定・更新する。スキル本体は`task-agent/.claude/skills/progress-update/SKILL.md`（トリガー文言「進捗を更新して」）。既にいずれかの項目が入力済みのレコードは人手確認済みとみなし上書きしないため、繰り返し実行しても安全。ステータスは`StatusSelect`が持つ6値（`未着手`/`依頼中`/`確認中`/`進行中`/`完了`/`-`）のいずれかに必ず正規化する。

**MCPツールの追加**: `task_agent.py`の`task-manager`サーバーに`list_history`（`repo.list_history`のラッパー）・`update_history_entry`（`repo.update_history_entry`のラッパー、4項目とも必須引数）を追加した。これに伴い、AgentCore実行ロールのDynamoDBポリシー（`task-agent/dynamodb-access-policy-clients.json`）に`TaskHistory`への`dynamodb:Query`/`dynamodb:UpdateItem`を追加している（4-4-1節と同様、IAMロールへのアタッチ自体はIaC管理外のため手動作業が必要）。

### 4-5. 履歴機能（2026-07-30〜31、`32dfa94`/`745c0d4`。2026-08-03に`seriesCode`/`frameCodes`・進捗反映機能を追加）

TaskとSeries×Frameの定型業務進捗とは別に、クライアントごとの自由記述の時系列ログを残す機能。`date`（日付）・`category`（分類、自由記述）・`assignee`（担当者）・`status`（ステータス、自由記述文字列でTaskの6段階ステータスとは無関係）・`content`（内容）の5項目に加え、`416629a`（2026-08-03）で`seriesCode`（タスク名、任意）・`frameCodes`（対象月、任意・複数選択可能な配列）を追加した。1クライアントに対して同一日付でも複数エントリを許容し、一覧は「分類昇順、同一分類内は日付昇順」でソートして返す（`list_history`内でクライアント側ソート、GSIは使わない）。

**編集UI（2026-08-03に刷新、`b5152c9`/`74a709b`/`b345d0f`/`0cd2304`/`ccf77f2`）**: 従来はテーブルセルのインライン編集だったが、テーブル行をクリックすると当該エントリがパネル上部の追加フォーム（`history__new`）に読み込まれ、フォーム上で編集して「更新する」ボタンで保存する方式に変更した（`editingId`が`null`なら新規追加モード、historyIdが入っていれば編集モード）。フォームの項目構成:
- 1行目: 日付・担当者・内容・CSVアップロードボタン
- 2行目: 分類（`taskGroup`のプルダウン）・タスク名（`Series`のプルダウン、`FrameMultiSelect.jsx`と同様チェックボックス式ではなく`<select>`）・対象月（`FrameMultiSelect.jsx`によるチェックボックス付きドロップダウンで複数選択）・ステータス（`StatusSelect`）・保存ボタン

分類（`taskGroup`）を選ぶと、タスク名の選択肢が`seriesList`のうち`taskGroup`が一致するものだけに絞り込まれる（`74a709b`）。分類を変更して選択中のタスク名がその分類に属さなくなった場合は、タスク名の選択を自動的にクリアする。

**進捗反映ボタン（新設、`b345d0f`）**: 編集モード（`editingId`あり）のときのみ表示される。押下すると、そのエントリの`seriesCode`・`frameCodes`（対象月、複数）・`status`を使って、選択されている各`frameCode`ごとに`api.updateTask(clientCode, seriesCode, frameCode, {status})`を呼び出し、対応するTaskレコードのステータスを一括更新する（`Promise.allSettled`で並列実行し、一部失敗してもエラー件数を表示しつつ処理は継続）。`seriesCode`または`frameCodes`が未選択の場合はAPIを呼ばずエラーメッセージを表示する。成功後は`onTasksChanged`コールバック（`App.jsx`から`refresh(client.clientCode)`が渡される）でタスク一覧・進捗表を再取得させる。**Bedrock AgentCoreの`progress`エージェント（4-4-2節、CSV取り込み後の`content`をLLMが解釈して履歴4項目を自動判定するもの）とは別物**で、こちらは既に手入力・確定済みの履歴エントリの内容を、担当者が手動でワンクリックによりTask側のステータスへ反映するための機能（9-12節参照）。

**表示整形（`0cd2304`）**: テーブル上の`date`は内部的にはISO形式（`YYYY-MM-DD`）を保持したまま、表示のみ`formatDateDisplay()`で`MM/DD`形式に短縮する。`frameCodes`（複数の対象月）は`formatFrameCodesDisplay()`で年月順に並べ替えたうえで連続する月を「開始-終了月」の範囲表記にまとめる（例:「2月・3月・4月」→「2-4月」、「1月・3月・5月・6月」→「1,3,5-6月」）。

加えて`390a3ea`でCSVアップロード機能を追加した: 外部ライブラリを使わない自前のCSVパーサ（`HistoryPanel.jsx`内、引用符エスケープ・改行コード両対応）でファイルをパースし、**行ごとに`POST /clients/{clientCode}/history`を逐次呼び出す**（バッチAPIは存在しない）。CSVの各セルは半角スペースで連結して`content`にそのまま格納され、`date`/`category`/`seriesCode`/`frameCodes`/`assignee`/`status`は空のまま登録される — 取り込み後に手動で分類・日付・タスク名・対象月等を埋める運用を前提とした設計。

MCPツールとしては`list_history`/`create_history_entry`/`update_history_entry`の3つのみが公開されており（`5c9803f`）、`delete_history_entry`はMCP経由では呼べない。`create_history_entry`/`update_history_entry`はレイヤ関数のシグネチャをそのままMCPツールとして公開する実装（6-2節）のため、`416629a`で`layer/python/task_repository.py`側に`series_code`/`frame_codes`引数を追加したことで、`mcp_src/app.py`自体には一切手を加えていないにもかかわらずMCPツールの入力スキーマにも自動的に`series_code`/`frame_codes`が追加されている。

なお、`content`の自由記述文をキーワード／正規表現ルールで自動分類する「分類機能」を4-7節にまとめている（2026-08-03にコード実装済み、AWS未デプロイ）。

### 4-6. 用語の変遷

初回コミット時点では「顧客」を表す`customerName`というキーが使われていたが、`clientCode`/`clientName`への改名を経て、最終的に「案件（Case）」という中間概念を廃し「クライアント（Client）」に一本化した（詳細経緯は旧設計書参照、13章）。

UI表示名と内部コード名が一致しない属性がある点に注意: `Series`はUI上「タスク」、`Frame`はUI上「月」、`taskGroup`はUI上「分類」と表示される（`7d352b2`/`ddb114f`/`b0a9b9c`等の一連のUI文言変更コミットによる）。API・DynamoDB上のフィールド名（`seriesCode`/`frameCode`/`taskGroup`）は変更していない。

`skill/bpo-task-manager/SKILL.md`という命名から、"BPO"（業務委託代行）＝税理士事務所などの外部委託業務向けであることが裏付けられる。

### 4-7. 分類機能（2026-08-03実装・デプロイ済み）

タスク履歴の`content`（自由記述の内容欄）を、キーワード／正規表現ルールに基づいて自動分類する機能。既存の`taskGroup`（Seriesマスタに紐づく単一選択の分類、進捗表の集約キー）とは別に、履歴エントリの内容そのものを複数の観点（軸）から多面的に分類する用途を想定している。テーブル定義・バックエンドロジック・API・フロントエンド画面まで一式実装し、`task-tables`（`TaskClassificationRules`テーブル実体作成）・`taskmanager`（分類機能関連の環境変数・IAMポリシー・APIルート）の両スタックとも`aws cloudformation deploy`/`sam deploy`済み。Web UIの「分類ルール」ページで軸・ルールの作成、履歴パネルの「自動判定」ボタンの動作を確認済み。

#### 4-7-1. データ構造（単一テーブル設計）

`TaskClassificationRules`テーブル（`tables.yaml`）1つに、「軸（Axis）」の定義と、各軸に属する「ルール（Rule）」の両方を格納する（詳細スキーマは5-8節）。

- **軸（Axis）**: 分類の観点そのもの。例:「taskGroup」（値: 三井住友銀行／群馬銀行／給与／借入／売上／支払）、「taskSeries」（値: 資料受領／資料分類／処理登録／確認）。`axisId`と表示用`label`のみを持つ。
- **ルール（Rule）**: ある軸に属し、`content`の文字列がどう判定されれば`category`（分類値）になるかを表す。`pattern`（キーワードまたは正規表現）・`matchType`（`keyword`=部分一致 / `regex`=正規表現）・`priority`（値が小さいほど優先して評価）を持つ。

軸はいくつでも追加でき、テーブル追加やデプロイは不要（既存のSeries/Frameマスタと同じ`lookupBucket`によるバケット分割パターンを踏襲、9-13節参照）。画面の「分類ルール」ページ（`ClassificationAxesPage.jsx`）の「＋ 軸を追加」から作成する。

#### 4-7-2. 判定ロジック

軸ごとに、登録済みルールを`priority`の小さい順に評価し、最初に一致したルールの`category`を採用する（どのルールにも一致しなければ当該軸は`category: null`として結果に含める）。判定方式は「キーワード（部分一致、Python文字列の`in`演算子）」「正規表現（`re.search`）」の2種類。軸同士は完全に独立しており、ある軸の判定結果が他の軸の判定に影響を与えない。不正な正規表現パターンは`re.error`を捕捉して「不一致」として扱い、他のルール・他の軸の判定を止めない（`layer/python/task_repository.py`の`_rule_matches()`）。

LLM（Bedrock AgentCore）は使わず、同期的なルールベースの軽量な文字列マッチングのみで完結する点が、4-4-2節の`progress`エージェント（LLMによる文面解釈）とは異なる。

#### 4-7-3. API（`api_src/app.py`に実装済み）

| Method | Path | 処理 |
|---|---|---|
| GET/POST | `/classification-axes` | 軸の一覧取得・新規作成 |
| PATCH/DELETE | `/classification-axes/{axisId}` | 軸の更新・削除（削除時は配下のルールもカスケード削除） |
| GET/POST | `/classification-axes/{axisId}/rules` | 軸配下のルールの一覧取得・新規作成 |
| PATCH/DELETE | `/classification-axes/{axisId}/rules/{ruleId}` | ルールの更新・削除 |
| POST | `/classify` | body: `text`。登録済み全軸を1回でまとめて判定し`{results: [{axisId, label, category, matchedRuleId, matchedPattern}, ...]}`を返す（同期・高速、Lambda内で完結） |

いずれもエージェントジョブ関連API（4-4節）と同様、**MCPツールとしては公開しておらず、Web UI専用の機能**（`mcp_src/app.py`の`TOOL_FUNCTIONS`には追加していない）。

#### 4-7-4. フロントエンド（実装済み）

- **「分類ルール」ページ**（`ClassificationAxesPage.jsx`、新設。既存の`ClientListPage`/`SeriesListPage`/`FrameListPage`と同様、ハンバーガーメニュー配下の独立画面）: 軸タブで切り替え、軸の追加・改名（表示名の入力欄からフォーカスアウトで即時保存）・削除、軸ごとのルール一覧・編集フォームを持つ。ルールの編集は`HistoryPanel.jsx`の履歴一覧と同じ「行クリックでフォームに読み込んで編集」方式（4-5節）を踏襲している。
- **`HistoryPanel.jsx`**: 「内容」入力欄の隣に「自動判定」ボタンを追加した。押下時に`content`を`POST /classify`へ渡し、登録済み全軸を判定した結果を、軸の数だけ動的に描画される入力欄（軸ラベル付きのテキスト入力、手動での上書きも可能）へ反映する。軸一覧自体は`HistoryPanel`表示時に`GET /classification-axes`で1回だけ取得する（クライアント横断の共通マスタのため）。
- **Historyエントリのデータモデル拡張**: 既存の`category`（taskGroupセレクトの値、4-1節）に加えて、`classifications`（軸ID→分類名のマップ）属性を追加した（5-5節参照）。

#### 4-7-5. 初期投入が必要な軸・分類値

以下は本番投入時に「分類ルール」ページから手動で登録する想定の軸・分類値（テーブル定義やシードスクリプトとしては存在しない。マスタ自動登録の対象外）。

| 軸ID | 分類値 |
|---|---|
| taskGroup | 三井住友銀行、群馬銀行、給与、借入、売上、支払（支払には請求書・領収書のキーワードも追加） |
| taskSeries | 資料受領、資料分類、処理登録、確認 |

---

## 5. データモデル設計（DynamoDB）

DynamoDB（NoSQL、ORM・マイグレーション不使用、`boto3.resource("dynamodb")`を直接操作）。`tables.yaml`でLambdaスタック（`taskmanager`）とは別のCloudFormationスタック（`task-tables`）として管理。**7テーブル構成**（旧4テーブルに`TaskHistory`/`TaskAgentJobs`/`TaskClassificationRules`を追加。`TaskClassificationRules`は2026-08-03に追加・デプロイ済み、4-7節参照）、いずれも`BillingMode: PAY_PER_REQUEST`、**GSIは1つも持たない**。

### 5-1. Tasks テーブル

```
PK: clientCode (String)
SK: taskKey (String, "{seriesCode}#{frameCode}" の連結文字列, 例 "SR001#202606")
属性: seriesCode, frameCode, status, assignee, completeDate, createdAt, updatedAt
```
`Query(PK=clientCode)`でクライアント別タスク一覧を取得。SKを`{seriesCode}#{frameCode}`の連結文字列にすることで、追加のインデックスなしに「シリーズ内でフレーム順」の並びが手に入る（`frameCode`が`YYYYMM`形式のため同一シリーズ内は時系列順）。単一タスクの取得・更新は`GetItem`/`UpdateItem(PK=clientCode, SK=taskKey)`で完結。「あるフレーム・あるシリーズを全クライアント横断で一覧する」というクエリは不要と判断したため、GSIは持たない。

### 5-2. TaskClients テーブル（クライアントマスタ）

```
PK: lookupBucket (String, 固定値 "CLIENT")
SK: clientCode (String)
属性: clientName, receiptFolderId(任意), renamedFolderId(任意)
```
`Query(PK="CLIENT")`で登録済み全クライアントを一覧取得できる（クライアント選択ドロップダウン用）。`receiptFolderId`/`renamedFolderId`は4-4節のエージェント機能用のGoogle DriveフォルダID（未設定時は属性自体を書き込まない）。

### 5-3. TaskSeries テーブル（シリーズマスタ、UI表示名「タスク」）

```
PK: lookupBucket (String, 固定値 "SERIES")
SK: seriesCode (String)
属性: seriesName, taskGroup(表示名「分類」)
```
`Query(PK="SERIES")`で登録済み全シリーズを一覧取得（タスク作成時のドロップダウン用）。`taskGroup`は`ddb114f`（2026-07-28）で追加。

### 5-4. TaskFrames テーブル（フレームマスタ、UI表示名「月」）

```
PK: lookupBucket (String, 固定値 "FRAME")
SK: frameCode (String, 例 "202606")
属性: frameName
```
`Query(PK="FRAME")`で登録済み全フレームを一覧取得。`frameCode`を`YYYYMM`のゼロ埋め文字列にしているため、文字列ソートがそのまま時系列順になる。

### 5-5. TaskHistory テーブル（履歴、新設）

```
PK: clientCode (String)
SK: historyId (String, UUID)
属性: date, category, seriesCode(任意), frameCodes(任意, 配列), assignee, status, content, createdAt, updatedAt
```
`Query(PK=clientCode)`でクライアントの全履歴を取得し、`list_history()`内でクライアント側（Lambda側）ソート（分類→日付→作成日時の順）する。GSIなし。TTL設定なし（永続保持）。`seriesCode`/`frameCodes`は`416629a`（2026-08-03）で追加され、履歴エントリを特定のTask（Series×Frame）に紐付けて進捗反映（4-5節）に使うための任意項目。

### 5-6. TaskAgentJobs テーブル（エージェントジョブ、新設）

```
PK: clientCode (String)
SK: jobId (String, UUID)
属性: agentId, status(processing/completed/error), prompt, result または error, claimedAt, createdAt, updatedAt, ttl
```
`TimeToLiveSpecification`（属性`ttl`、`Enabled: true`）により、`AGENT_JOB_TTL_SECONDS`（1日）経過後にDynamoDB側が自動削除する。GSIなし。`claimedAt`は4-4節の二重実行防止用。

### 5-7. マスタ自動登録の一貫性

Series/Frameは、`ConditionExpression: attribute_not_exists(...)`付き`PutItem`で冪等に登録し、`ConditionalCheckFailedException`（＝既存）は正常系として握りつぶすパターンを共通で使用（`_ensure_series_exists()` / `_ensure_frame_exists()`）。これはタスク作成時の自動登録専用の内部ヘルパーであり、4-3節で追加された明示的な`create_series()`/`create_frame()`（既存コード指定時は`SeriesAlreadyExistsError`/`FrameAlreadyExistsError`を送出）とは別物として使い分けられている。Clientは元からこのサイレント登録パターンを使わず、`create_client()`で明示的にエラーを返す（9-2参照）。

### 5-8. TaskClassificationRules テーブル（分類ルール、2026-08-03デプロイ済み）

```
PK: lookupBucket (String, "AXIS" または "RULE#<axisId>")
SK: sortKey (String, AXISバケットではaxisId、RULE#<axisId>バケットではruleId(UUID)の値をそのまま入れる)
属性:
  AXISバケット: axisId, label
  RULE#<axisId>バケット: ruleId, axisId, category, pattern, matchType(keyword/regex), priority
```
DynamoDBのRangeKeyは1テーブルにつき単一のアトリビュート名を共有する必要があるため、物理的なSK属性名は`sortKey`に統一し、`axisId`/`ruleId`はアプリ側で読みやすいよう別属性としても複製して持たせている（`layer/python/task_repository.py`）。`Query(PK="AXIS")`で全軸一覧、`Query(PK="RULE#<axisId>")`で特定の軸配下の全ルールを取得し、`priority`昇順にLambda側でソートする（5-2〜5-4節のSeries/Frameマスタと同じ`lookupBucket`によるバケット分割パターンを踏襲。ソート方式は5-5節のTaskHistoryと同様のクライアント側ソート）。軸削除時は同一`lookupBucket="RULE#<axisId>"`配下のルールを一括`DeleteItem`する（カスケード削除）。GSIなし。詳細は4-7節参照。`tables.yaml`への追加後、2026-08-03に`aws cloudformation deploy`済みで実テーブルが存在する。

---

## 6. バックエンド設計

### 6-1. `layer/python/task_repository.py` — 共通CRUDロジック（ビジネスロジックの中核）

Web API・MCPサーバー・エージェントジョブ処理Lambdaの3つから`import task_repository as repo`で参照される、フレームワーク非依存のプレーンな関数群（768行）。8個だった公開関数は23個まで増えており、2026-08-03の分類機能追加（4-7節）でさらに9個増え32個になった。

**クライアント**
| 関数 | 役割 |
|---|---|
| `list_clients()` | `Query(PK="CLIENT")`で全クライアント返す |
| `create_client(client_code, client_name, receipt_folder_id=None, renamed_folder_id=None)` | 冪等でなく、既存なら`ClientAlreadyExistsError` |
| `update_client(client_code, client_name=None, receipt_folder_id=None, renamed_folder_id=None)` | 指定項目のみ`UpdateExpression`で更新。未存在なら`ClientNotFoundError` |
| `delete_client(client_code)` | 単純削除 |

**シリーズ**
| 関数 | 役割 |
|---|---|
| `list_series()` | `Query(PK="SERIES")`で全シリーズ返す |
| `create_series(series_code, series_name, task_group="")` | 既存なら`SeriesAlreadyExistsError`（明示登録専用、自動登録の`_ensure_series_exists`とは別） |
| `delete_series(series_code)` | 単純削除 |

**フレーム**
| 関数 | 役割 |
|---|---|
| `list_frames()` | `Query(PK="FRAME")`で全フレーム返す（`frameCode`昇順で自然に時系列順） |
| `create_frame(frame_code, frame_name)` | 既存なら`FrameAlreadyExistsError` |
| `delete_frame(frame_code)` | 単純削除 |

**タスク**
| 関数 | 役割 |
|---|---|
| `list_tasks_by_client(client_code)` | `Query(PK=clientCode)`でタスク一覧（SK順＝シリーズ内でフレーム順） |
| `get_task(client_code, series_code, frame_code)` | `GetItem`。存在しなければ`TaskNotFoundError` |
| `create_task(client_code, series_code, series_name, frame_code, frame_name, status, assignee, complete_date, task_group="")` | `_ensure_series_exists()`/`_ensure_frame_exists()`を経て`PutItem`。同じ複合キーが既存でも単純に上書きする。`task_group`はシリーズが新規登録される場合のみ有効 |
| `update_task(client_code, series_code, frame_code, status, assignee, complete_date)` | 指定フィールドのみ`UpdateExpression`で動的構築 |

**履歴**
| 関数 | 役割 |
|---|---|
| `list_history(client_code)` | `Query(PK=clientCode)`し分類→日付→作成日時の順にクライアント側ソート |
| `create_history_entry(client_code, date, category="", series_code="", frame_codes=None, assignee="", status="", content="", classifications=None)` | `historyId`をUUIDで発番しPutItem。`series_code`/`frame_codes`は`416629a`、`classifications`（軸ID→分類名のマップ）は2026-08-03の分類機能追加で追加（いずれも任意） |
| `update_history_entry(client_code, history_id, ...)` | 指定項目のみ更新。`series_code`/`frame_codes`/`classifications`も更新可能。未存在なら`HistoryEntryNotFoundError` |
| `delete_history_entry(client_code, history_id)` | 単純削除 |

**エージェントジョブ**
| 関数 | 役割 |
|---|---|
| `create_agent_job(client_code, agent_id, prompt)` | `jobId`をUUIDで発番し`status="processing"`・`ttl`（1日後）付きでPutItem |
| `get_agent_job(client_code, job_id)` | ポーリング用GetItem。未存在なら`AgentJobNotFoundError` |
| `claim_agent_job(client_code, job_id)` | `ConditionExpression="attribute_not_exists(claimedAt)"`の条件付き更新。既にclaim済みなら`False`（4-4節） |
| `complete_agent_job(client_code, job_id, result)` | `status="completed"`+結果を書き込み |
| `fail_agent_job(client_code, job_id, error)` | `status="error"`+エラー内容を書き込み |

**分類ルール（2026-08-03追加、4-7節）**
| 関数 | 役割 |
|---|---|
| `list_classification_axes()` | `Query(PK="AXIS")`で全軸返す |
| `create_classification_axis(axis_id, label)` | 既存なら`ClassificationAxisAlreadyExistsError` |
| `update_classification_axis(axis_id, label=None)` | `label`のみ更新。未存在なら`ClassificationAxisNotFoundError` |
| `delete_classification_axis(axis_id)` | 配下の`RULE#<axisId>`ルールを先に全削除してから軸自体を削除（カスケード削除） |
| `list_classification_rules(axis_id)` | `Query(PK="RULE#<axisId>")`し`priority`昇順にクライアント側ソート |
| `create_classification_rule(axis_id, category, pattern, match_type="keyword", priority=0)` | 軸が未登録なら`ClassificationAxisNotFoundError`、`match_type`が`keyword`/`regex`以外なら`ValueError` |
| `update_classification_rule(axis_id, rule_id, ...)` | 指定項目のみ更新。未存在なら`ClassificationRuleNotFoundError` |
| `delete_classification_rule(axis_id, rule_id)` | 単純削除 |
| `classify(text)` | 登録済み全軸を`priority`昇順ルール評価で判定し`{axisId, label, category, matchedRuleId, matchedPattern}`のリストを返す。不一致の軸は`category=None` |

`_task_key()`（SKの連結文字列生成）、`_ensure_series_exists()`/`_ensure_frame_exists()`（サイレント冪等登録）は変更なし。エラークラスは上記7種に加え`ClassificationAxisAlreadyExistsError`/`ClassificationAxisNotFoundError`/`ClassificationRuleNotFoundError`を加えた10種。

### 6-2. `mcp_src/app.py` — MCPサーバー（Claude Desktop / claude.ai向け）

`FastMCP("task-manager", stateless_http=True, json_response=True, streamable_http_path="/")` によるStreamable HTTP方式のMCPサーバー。`TOOL_FUNCTIONS`に登録されたMCPツールは**11個**（旧8個から履歴系3関数を追加、`5c9803f`）:

```
list_clients / create_client / list_series / list_frames /
list_tasks_by_client / get_task / create_task / update_task /
list_history / create_history_entry / update_history_entry
```

**MCP経由では利用できない機能**（Web UI専用）: `update_client`/`delete_client`、`create_series`/`delete_series`、`create_frame`/`delete_frame`、`delete_history_entry`、エージェントジョブ関連の5関数すべて、分類ルール関連の9関数すべて（4-7節）。つまりClaude側からは、クライアントの新規作成・タスクのCRUD・履歴の閲覧と追加/更新はできるが、マスタの削除やエージェント機能・分類機能の起動・管理はできない。ただし`create_history_entry`/`update_history_entry`はMCPツールとして公開済みのため、`416629a`のときと同様、`classifications`パラメータもレイヤ関数のシグネチャ経由でMCPツールの入力スキーマに自動的に含まれている。

**Lambda呼び出しごとにアプリを再構築する設計**（変更なし）: `StreamableHTTPSessionManager.run()`はインスタンスごとに1回しか呼べない制約があるため、`FastMCP`インスタンスをモジュールレベルで使い回さず、`handler(event, context)`の呼び出し毎に`build_app()`で新規構築している。実行時は`Mangum(app, lifespan="auto")`でASGI→Lambdaイベント変換。

**認可（`AccessControlMiddleware`）**（変更なし）: Function URL自体の`AuthType`は`NONE`だが、アプリ内ミドルウェアで以下のいずれかを満たさない限り401を返す。
1. `Authorization: Bearer <MCP_AUTH_TOKEN>` が環境変数値と一致（Claude Desktop + mcp-remote経由）
2. 送信元IPがAnthropicの公開アウトバウンドIPレンジ（`160.79.104.0/21`）内（claude.ai/モバイルのカスタムコネクタ経由）

### 6-3. `api_src/app.py` — Web API（社内フロントエンド向け）

Flask/FastAPI等のフレームワークは使用せず、`event["routeKey"]`による素朴なif分岐でルーティングを自前実装している（344行）。`lambda_client = boto3.client("lambda")`を追加保持し、エージェントジョブの起動に使う。

| Method | Path | 処理 | 備考 |
|---|---|---|---|
| GET | `/clients` | `repo.list_clients` | クライアント選択ドロップダウン用 |
| POST | `/clients` | `repo.create_client` | body: `clientCode,clientName,receiptFolderId?,renamedFolderId?`。既存なら409 |
| PATCH | `/clients/{clientCode}` | `repo.update_client` | body: `clientName?,receiptFolderId?,renamedFolderId?`。未存在なら404 |
| DELETE | `/clients/{clientCode}` | `repo.delete_client` | |
| GET | `/series` | `repo.list_series` | タスク作成時のドロップダウン用 |
| POST | `/series` | `repo.create_series` | body: `seriesCode,seriesName,taskGroup?`。既存なら409 |
| DELETE | `/series/{seriesCode}` | `repo.delete_series` | |
| GET | `/frames` | `repo.list_frames` | 時系列順 |
| POST | `/frames` | `repo.create_frame` | body: `frameCode,frameName`。既存なら409 |
| DELETE | `/frames/{frameCode}` | `repo.delete_frame` | |
| GET | `/clients/{clientCode}/tasks` | `repo.list_tasks_by_client` | シリーズ→フレーム順にソート済み |
| GET | `/tasks/{clientCode}/{seriesCode}/{frameCode}` | `repo.get_task` | 複合キーをパスパラメータ3つで表現 |
| POST | `/tasks` | `repo.create_task` | body: `clientCode,seriesCode,seriesName,frameCode,frameName,status?,assignee?,completeDate?,taskGroup?` |
| PATCH | `/tasks/{clientCode}/{seriesCode}/{frameCode}` | `repo.update_task` | body: `status?,assignee?,completeDate?` |
| GET | `/clients/{clientCode}/history` | `repo.list_history` | ソート済み |
| POST | `/clients/{clientCode}/history` | `repo.create_history_entry` | body: `date`必須、`category?,seriesCode?,frameCodes?,assignee?,status?,content?,classifications?`（`seriesCode`/`frameCodes`は`416629a`、`classifications`は2026-08-03の分類機能追加で追加） |
| PATCH | `/clients/{clientCode}/history/{historyId}` | `repo.update_history_entry` | 未存在なら404 |
| DELETE | `/clients/{clientCode}/history/{historyId}` | `repo.delete_history_entry` | |
| POST | `/clients/{clientCode}/agent-jobs` | `repo.create_agent_job` + `TaskAgentJobProcessorFunction`をEvent非同期invoke | body: `agentId,prompt`。202を返す |
| GET | `/clients/{clientCode}/agent-jobs/{jobId}` | `repo.get_agent_job` | ポーリング用。未存在なら404 |
| GET | `/classification-axes` | `repo.list_classification_axes` | 「分類ルール」ページの軸タブ用 |
| POST | `/classification-axes` | `repo.create_classification_axis` | body: `axisId,label`。既存なら409 |
| PATCH | `/classification-axes/{axisId}` | `repo.update_classification_axis` | body: `label?`。未存在なら404 |
| DELETE | `/classification-axes/{axisId}` | `repo.delete_classification_axis` | 配下のルールもカスケード削除 |
| GET | `/classification-axes/{axisId}/rules` | `repo.list_classification_rules` | `priority`昇順 |
| POST | `/classification-axes/{axisId}/rules` | `repo.create_classification_rule` | body: `category,pattern,matchType?,priority?`。軸未登録なら404 |
| PATCH | `/classification-axes/{axisId}/rules/{ruleId}` | `repo.update_classification_rule` | 未存在なら404 |
| DELETE | `/classification-axes/{axisId}/rules/{ruleId}` | `repo.delete_classification_rule` | |
| POST | `/classify` | `repo.classify` | body: `text`必須。`HistoryPanel.jsx`の「自動判定」ボタン用 |

- CSVアップロード専用のバックエンドエンドポイントは存在しない。フロントエンドがCSVをパースし、行ごとに通常の`POST /clients/{clientCode}/history`を逐次呼び出す（4-5節）。
- レスポンスは`_response()`ヘルパーでCORSヘッダとJSON形式（`ensure_ascii=False`）を統一。
- エラーは`ClientAlreadyExistsError`/`SeriesAlreadyExistsError`/`FrameAlreadyExistsError`/`ClassificationAxisAlreadyExistsError`→409、`ClientNotFoundError`/`TaskNotFoundError`/`HistoryEntryNotFoundError`/`AgentJobNotFoundError`/`ClassificationAxisNotFoundError`/`ClassificationRuleNotFoundError`→404、`KeyError`/`ValueError`→400、その他`Exception`→500の4段階。
- JWT検証はこのLambda自身では行わず、API Gateway側の`CognitoAuth`オーソライザーに委譲している（変更なし）。

### 6-4. `agent_job_src/app.py` — エージェントジョブ処理Lambda（新設）

`TaskApiFunction`から`InvocationType="Event"`で直接起動される、API Gatewayを経由しない非同期処理専用のLambda（`Timeout: 900`秒）。処理内容・設計上の注意点は4-4節を参照。要点のみ再掲:

- Lambdaの非同期呼び出しの自動リトライ対策として、実行の先頭で`claim_agent_job()`による条件付き書き込みclaimを行い、重複実行（＝Drive側への二重の副作用）を防ぐ。
- `bedrock-agentcore`クライアントは`read_timeout=850`秒・`retries={"max_attempts": 1}`という明示的な設定を持つ。botocoreの既定値のままだとAgentCoreの実行時間より短いタイムアウトでリトライが起こり、同一セッションへの二重リクエストにつながるため。
- `Tasks`/`TaskHistory`テーブルへの書き込みは一切行わない。書き込み先は`TaskAgentJobs`のみ（`complete_agent_job`/`fail_agent_job`）。

### 6-5. メール連携機能の廃止（変更なし）

旧モデルにあった`sourceThreadIds`（Gmailスレッド連携）・`link_email`は再設計時に完全に廃止済み。`skill/SKILL.md` / `skill/bpo-task-manager/SKILL.md`にはメール連携についての記述がまだ残っており、別途見直しが必要（12章参照）。README・SKILL.mdで言及されている`email_router.py`は本リポジトリに存在しない外部連携スクリプト。

---

## 7. フロントエンド設計

### 7-1. コンポーネント構成（`frontend/src/`）

18ファイル（旧8ファイルから倍増、2026-08-03に`ClassificationAxesPage.jsx`を追加）:

```
main.jsx               エントリポイント。Amplify.configure() → Authenticator（日本語UIラベルにカスタマイズ）でログイン画面をラップ
App.jsx                ルートコンポーネント。ハンバーガーメニューによる画面(view)切り替え + パネル内タブ(tab)切り替えの2階層構成
NavMenu.jsx             ハンバーガーメニュー。進捗(ホーム)/クライアント/タスクシリーズ/フレーム/分類ルールの5項目(2026-08-03に「分類ルール」を追加)
ClientListPage.jsx      クライアントマスタの一覧・編集・新規作成・削除画面(receiptFolderId/renamedFolderIdも編集可)
SeriesListPage.jsx      シリーズ(表示名「タスク」)マスタの一覧・編集・新規作成・削除画面(taskGroupも編集可)
FrameListPage.jsx       フレーム(表示名「月」)マスタの一覧・編集・新規作成・削除画面
ClassificationAxesPage.jsx  「分類ルール」ページ(2026-08-03新設)。軸タブ切り替え+軸の追加・改名・削除+軸ごとのルール一覧・編集フォーム(行クリックで編集フォームへ読み込む方式、4-5節のHistoryPanel.jsxと同じUX)、4-7節
ClientSelector.jsx      パネルヘッダー内のクライアント選択ドロップダウン(旧ClientSidebar.jsxを置き換え)
ProgressTable.jsx       進捗表(ピボットテーブル)。行=シリーズ、列=フレーム、セル=ステータススタンプ。デフォルト表示タブ
TaskTable.jsx           タスク一覧のフラット表示テーブル。担当はインライン編集、状態はStatusSelect
TaskDetailPanel.jsx     タスク選択時に開く右側詳細パネル
NewTaskForm.jsx         既存シリーズをドロップダウン選択し、全登録フレーム分のタスクを一括作成するフォーム
StatusSelect.jsx        6段階(未着手/依頼中/確認中/進行中/完了/-)のドロップダウン
FrameMultiSelect.jsx    対象月(Frame)をチェックボックスで複数選択するドロップダウン(2026-08-03新設、HistoryPanel.jsxの対象月入力用)
HistoryPanel.jsx        履歴タブ。上部フォーム+CSVアップロード。行クリックでフォームに読み込んで編集する方式(2026-08-03にインライン編集から刷新、4-5節)
AgentsPanel.jsx         エージェントタブ。ジョブ発行・ポーリング・結果表示
api.js                  fetchベースのAPIクライアント(Cognitoトークン付与)、28エクスポート関数
config.js                デプロイ後の値(Cognito ID・APIエンドポイント)を反映する静的設定ファイル
```

### 7-2. `App.jsx` の画面構造

2階層のstate駆動画面遷移（React Router等は不使用）:

1. **`currentView`**（`NavMenu`のハンバーガーメニューで切り替え）: `"進捗"`（デフォルト・ホーム）/`"クライアント"`/`"タスクシリーズ"`/`"フレーム"`/`"分類ルール"`（2026-08-03追加）。後者4つはそれぞれ`ClientListPage`/`SeriesListPage`/`FrameListPage`/`ClassificationAxesPage`という独立したマスタ編集専用の全面画面になる。
2. **`activeTab`**（`currentView === "進捗"`のときのみ有効）: `TABS = ["進捗", "履歴", "タスク", "エージェント"]`（`bf803bb`でデフォルトが「進捗」に変更済み）。それぞれ`ProgressTable`/`HistoryPanel`/（`TaskTable`+`NewTaskForm`）/`AgentsPanel`に対応する。

主な状態: `client`（デフォルト`{clientCode:"MM", clientName:"MM株式会社"}`）、`tasks`、`seriesList`、`frameList`、`selectedTaskKey`、`activeTab`、`menuOpen`、`currentView`。`seriesNameByCode`/`frameNameByCode`/`seriesGroupByCode`の3つのコード→表示名変換マップを`useMemo`で保持し、`ProgressTable`/`TaskTable`の表示に使う（旧設計書7-3節の仕組みを踏襲、`seriesGroupByCode`が新規追加）。

### 7-3. `api.js` の構成

28個のエクスポート関数を持つ（旧7個から増加。2026-08-03に分類機能関連の9関数を追加）:

```js
export const api = {
  listClients, createClient, updateClient, deleteClient,
  listSeries, createSeries, deleteSeries,
  listFrames, createFrame, deleteFrame,
  listTasksByClient, createTask, updateTask,
  listHistory, createHistoryEntry, updateHistoryEntry, deleteHistoryEntry,
  submitAgentJob, getAgentJob,
  listClassificationAxes, createClassificationAxis, updateClassificationAxis, deleteClassificationAxis,
  listClassificationRules, createClassificationRule, updateClassificationRule, deleteClassificationRule,
  classify,
};
```
`authHeader()`（Cognito IDトークン付与）と`request()`（fetchラップ＋エラー変換）という共通ヘルパー構造は維持。`GET /tasks/{clientCode}/{seriesCode}/{frameCode}`（単一タスク取得）のみ、フロントエンドが使わないため`api.js`に対応関数がない（クライアントの全タスクを毎回まとめて取得する設計のため不要、YAGNI）。

### 7-4. マスタ名の解決（変更なし、7-2節参照）

`GET /clients/{clientCode}/tasks`が返すタスクは`seriesCode`/`frameCode`のみを持ち、表示用の名称を含まない。`App.jsx`が`listSeries()`/`listFrames()`の結果からコード→名称の変換マップを作成し、各表示コンポーネントへpropsで渡す。

### 7-5. 状態管理・同期方式（変更なし）

- 状態管理: Redux等は不使用。`App.jsx`トップレベルの`useState`をpropsで配布する素朴な構成。
- 疑似リアルタイム同期: `POLL_INTERVAL_MS = 4000`で4秒間隔ポーリングを継続（タスク一覧）。エージェントジョブのポーリングは別途`AgentsPanel.jsx`内で`3000ms`間隔・最大100回（4-4節）。
- 楽観的UI更新: `handleUpdate`はAPIコール前にローカル状態を即時更新し、失敗時は`refresh()`でサーバー状態に揃え直す。

### 7-6. デザイン方針（変更なし）

社内のタックス事務所向け業務ツールという位置付けから、SaaS風ではなく「台帳・帳簿」をモチーフにした配色・タイポグラフィを採用。ステータス表示は「印影（はんこ）」風の見た目（`.stamp`系CSSクラス）。CSS変数によるデザイントークン管理、フォントはShippori Mincho / Zen Kaku Gothic New / JetBrains Mono。

---

## 8. 認証・認可設計

2系統の認証方式が併存する（変更なし）。

### 8-1. Web UI向け（Cognito）
- User Pool: セルフサインアップ無効（`AdminCreateUserConfig.AllowAdminCreateUserOnly: true`）、管理者が`admin-create-user`でアカウント作成する運用。
- パスワードポリシー: 最小10文字、大文字/小文字/数字必須。
- API GatewayのHTTP APIに`CognitoAuth`というJWT Authorizerを全ルートのデフォルトとして適用（エージェントジョブ関連のエンドポイントも含め、新規追加ルートすべてに自動適用される）。
- フロントエンドは`fetchAuthSession()`からIDトークンを取得し`Authorization: Bearer <IDToken>`を全リクエストに付与。
- **RBACなし**: ロールベースのアクセス制御は未実装。認証されたユーザーは全員同じ権限を持つ（エージェント機能の起動権限も含め、区別なし）。

### 8-2. MCPサーバー向け（Bearer / IPホワイトリスト、変更なし）
Cognitoは使わず、`AccessControlMiddleware`による固定Bearerトークンまたは Anthropic公開IPレンジでの認可。トークンは`NoEcho: true`のSAMパラメータとして管理。

### 8-3. エージェントジョブ処理Lambda向けIAM
`TaskAgentJobProcessorFunction`はAPI Gatewayの背後になく、`TaskApiFunction`からのLambda Event invocationでのみ起動される（Function URLも持たない）。Bedrock AgentCoreへのアクセスは、`bedrock-agentcore:InvokeAgentRuntime`を`AgentRuntimeArn`本体と`${AgentRuntimeArn}/runtime-endpoint/*`の両方に許可するIAMステートメントで実現している（4-4節、`d47a54b`で修正）。

### 8-4. 既知のセキュリティ課題
`HttpApi.CorsConfiguration.AllowOrigins`が現状`"*"`のままであり、**本番運用前にAmplify Hostingの実ドメインへ限定する必要がある**（未対応、12章参照）。

---

## 9. 再設計における主要な意思決定

旧設計書（2026-07-27時点）で確定した意思決定に加え、履歴・エージェント機能の追加時に確定した判断を記す。

### 9-1〜9-6（旧設計書からの継続、要約）
- 9-1: 「案件（Case）」を廃し「クライアント（Client）」に統一。
- 9-2: Clientのみ`create_client`という明示的な登録手段を持つ（Series/Frameはタスク作成時の自動登録が基本だが、4-3節の通り2026-07-28以降は両者とも明示的なCRUD画面を追加で持つようになった。ただし「タスク作成時の自動登録」という非対称の原則自体は維持）。
- 9-3: タスクをClient×Series×Frameに分解。
- 9-4: `frameCode`を`YYYYMM`形式にした理由（文字列ソート＝時系列順）。
- 9-5: GSIを持たない設計（6テーブルに増えた現在も踏襲。TaskHistory/TaskAgentJobsもクライアント単位のクエリのみで足りるためGSIなし）。
- 9-6: 既存データを移行しなかった理由。

（各項目の詳しい経緯は本書の初版、Gitログの`d5c7c24`時点の`docs/設計書.md`を参照。データモデル自体は変わっていないため要点のみ再掲した。）

### 9-7. エージェント処理を「同期API＋非同期Lambda」の2段構成に分離した理由
API Gateway HTTP APIの統合タイムアウトは30秒が上限だが、Bedrock AgentCoreの実行（Google Driveのファイル走査・リネーム）は数十秒〜数分かかる。API Gatewayの背後にいる`TaskApiFunction`はジョブレコードの作成のみを同期的に行い、実際の長時間処理はAPI Gatewayを経由しない別Lambda（`TaskAgentJobProcessorFunction`、Timeout 900秒）にLambdaの`Event`非同期invocationで委譲する構成にした。フロントエンドはジョブIDをキーにポーリングして結果を取得する。

### 9-8. エージェントジョブに「claim」ステップを設けた理由
Lambdaの非同期(`Event`)呼び出しは、呼び出し先が例外を投げたりタイムアウトしたりすると既定で自動的に再試行する。何も対策をしないと、Bedrock AgentCore呼び出し（＝Google Drive上のファイルリネームという冪等でない副作用）が同一ジョブに対して複数回実行されるおそれがある。`claimed_at`属性への条件付き書き込み（`attribute_not_exists`）を「一度だけ勝てる」ゲートとして使うことで、この二重実行を防いだ。

### 9-9. エージェントジョブに1日のTTLを設定した理由
ジョブレコードは実行結果のポーリング用に一時的に必要なだけで、恒久的な監査ログとしての価値は低いと判断し、DynamoDBのTTL機能で1日後に自動削除する設計にした（対照的に、履歴（History）テーブルは業務ログとして永続保持し、TTLを設定していない）。

### 9-10. CSVアップロードを「行ごとに既存APIを叩く」実装にとどめた理由
バッチ専用のAPIエンドポイントを新設せず、フロントエンドで自前のCSVパーサを実装し、既存の`POST /clients/{clientCode}/history`を行数分繰り返し呼び出す実装にした。CSVの列と履歴エントリの項目（日付/分類/担当者/ステータス/内容）を1:1にマッピングする仕様が固まっていなかったため、まずは「各行をまるごと`content`に詰めて取り込み、後から手動で分類・日付を埋める」という最小実装で運用を開始し、列マッピングの精緻化は今後の課題とした（12章参照）。

### 9-11. Series/Frameマスタを後から編集可能にした理由
初期実装では「タスク作成時に未登録なら自動登録」のみでSeries/Frameを管理していたが、誤って登録してしまったコードの削除や、タスク作成前に事前にマスタを整備したいという運用上のニーズから、ハンバーガーメニュー配下に独立したCRUD画面（`SeriesListPage`/`FrameListPage`）を追加した。自動登録のフォールバック自体は運用の手間を減らすため残している。

### 9-12. 履歴に「進捗反映」ボタン（手動）を追加した理由（4-4-2節の`progress`エージェントとの役割分担）
4-4-2節の`progress`エージェント（Bedrock AgentCore経由）は、CSV一括取り込みで`content`しか埋まっていない履歴エントリに対し、LLMが文面を解釈して`date`/`category`/`assignee`/`status`の4項目を判定・埋める「未整形データの一次仕上げ」を担う。一方、担当者が普段の業務でその場で履歴を書きながら、対応するTaskのステータスも一緒に前進させたいという操作は、AgentCore呼び出し（非同期ジョブ・数秒〜数十秒のポーリング待ち）を経由するには不釣り合いに重い。そこで`b345d0f`では、履歴エントリにひも付く`seriesCode`/`frameCodes`が入力済みであれば、既存の同期API`PATCH /tasks/{clientCode}/{seriesCode}/{frameCode}`を選択された対象月の数だけ呼び出すだけの単純なボタンとして実装した。エージェントを介さないため即座に反映され、複数月をまとめて反映したい場合にも対応できる。

### 9-13. 分類ルールを軸ごとに別テーブルではなく単一テーブル（`TaskClassificationRules`）にまとめた理由（4-7節）
分類の観点（軸）の種類やルールの件数は、運用しながら試行錯誤で増減していくことが見込まれる。軸を追加するたびにテーブルやAPI・コードを増やす設計では、Series/Frameマスタで得た「使う側の画面操作だけでマスタを増減できる」という運用上の利点（4-3節）を損なう。そこで既存の`lookupBucket`によるバケット分割パターン（5-2〜5-4節）を踏襲し、`lookupBucket="AXIS"`（軸定義）と`lookupBucket="RULE#<axisId>"`（軸配下のルール）を1テーブルに同居させることで、軸の追加・削除を画面操作のみで完結させ、テーブル追加やデプロイを不要にした。また`POST /classify`で全軸をまとめて1回で判定できるようにしたのも、履歴パネルの「自動判定」ボタン押下時に軸の数だけAPIを呼び分ける必要をなくすための設計判断である。

---

## 10. インフラ・デプロイ構成

### 10-1. AWS SAM

- `task-tables`スタック（`tables.yaml`）: DynamoDB**7テーブル**（`Tasks`/`TaskClients`/`TaskSeries`/`TaskFrames`/`TaskHistory`/`TaskAgentJobs`/`TaskClassificationRules`）の独立スタック。Lambdaスタックとは疎結合のライフサイクル。`TaskClassificationRules`は2026-08-03に追加し、`aws cloudformation deploy`済み（4-7節・5-8節）。
- `taskmanager`スタック（`template.yaml`）: `TaskRepositoryLayer`（共通Layer）／`TaskMcpFunction`（物理名`taskmanager-mcp`、Function URL）／`TaskAgentJobProcessorFunction`（物理名`taskmanager-agent-job-processor`、Event非同期起動、Timeout 900秒）／`UserPool`（物理名`taskmanager-users`）+`UserPoolClient`（物理名`taskmanager-webapp`）／`HttpApi`（Cognito JWT Authorizer付き）／`TaskApiFunction`（物理名`taskmanager-api`、29イベント定義。2026-08-03に分類機能関連の9イベントを追加）を1スタックにまとめたメインスタック。スタック名は2026-08-01に`task-mcp-server`から`taskmanager`へ移行済み（10-3節参照）。分類機能追加分（環境変数・IAMポリシー・APIイベント）も2026-08-03に`sam deploy`済み。
- パラメータに`McpAuthToken`（既存）に加え、`AgentRuntimeArn`（デフォルト値`arn:aws:bedrock-agentcore:ap-northeast-1:155830630328:runtime/task_agent-lAAoax7UkB`。2026-08-02に`task-agent`移設・再デプロイに伴い旧`agent_agentcore-OTeMr39nYA`から更新、4-4-1節参照）がある。
- 3Functionとも`DynamoDBCrudPolicy`（SAM組み込みポリシーテンプレート）を7テーブルに付与（`TaskClassificationRules`分は2026-08-03追加）。`TaskApiFunction`はさらに`LambdaInvokePolicy`で`TaskAgentJobProcessorFunction`の起動権限を、`TaskAgentJobProcessorFunction`は`bedrock-agentcore:InvokeAgentRuntime`のカスタムIAMステートメントを持つ（8-3節）。
- `samconfig.toml`: `stack_name = "taskmanager"`, `s3_prefix = "taskmanager"`, `region = "ap-northeast-1"`, `capabilities = "CAPABILITY_IAM"`（`.gitignore`対象、リポジトリには含まれない）。

### 10-2. 再デプロイ時に発生したトラブルと対応（このマシン固有・恒久的な注記、2026-07-27時点、変更なし）

| 問題 | 原因 | 対応 |
|---|---|---|
| `task-tables`スタックが`UPDATE_ROLLBACK_COMPLETE`のまま実テーブルが消失していた | DynamoDBのキースキーマ変更はインプレース更新できず、ロールバック後にスタック状態と実リソースがズレる「ドリフト」が発生 | `task-tables`スタックを削除し、新`tables.yaml`で`CREATE_COMPLETE`として作り直した |
| AWS CLIが`SSL: CERTIFICATE_VERIFY_FAILED`で失敗 | このマシンではNorton Antivirusが常駐しHTTPS通信をSSL検査(MITM)しており、AWS CLI(Python/botocore)がそのルート証明書を信頼していない | `AWS_CA_BUNDLE`環境変数にNortonの証明書を指定 |
| `aws cloudformation deploy`実行時に`cp932`デコードエラー | `tables.yaml`のコメントに残っていた日本語文字がこのマシンのロケール(cp932)でデコードできなかった | コメントを英語に修正 |
| `sam build`（コンテナなし）が`pywin32`の解決に失敗 | `mcp`パッケージが`sys_platform == "win32"`条件で`pywin32`に依存しており、Windows上でのネイティブビルドがLambdaのLinuxターゲットと噛み合わず失敗 | `sam build --use-container`でLinuxコンテナ内ビルドに切り替え |
| コンテナ内`pip`が`SSL: CERTIFICATE_VERIFY_FAILED`でPyPIに到達できない | コンテナ内のpipもNortonのMITM証明書を信頼していない | `--container-env-var-file`で`PIP_TRUSTED_HOST`を注入 |

これらは全てこの開発マシン固有の環境要因であり、リポジトリのコード自体に問題があったわけではない。

### 10-3. スタック名移行とデプロイ実績

**2026-08-01: `task-mcp-server` → `taskmanager` へのスタック名移行**

バックエンドのメインスタック名が初期実装（MCPサーバー単体）当時の`task-mcp-server`のままだったため、リポジトリ名・フロントエンドのpackage名（`task-manager-frontend`）と揃える形で`taskmanager`に移行した。CloudFormationはスタック名を直接リネームできないため、以下の手順で「旧スタック削除→新スタック作成」による移行を実施した（短時間のダウンタイムを許容する前提で実施。DynamoDBの実データは別スタック`task-tables`のため無関係、Amplify Hostingアプリ自体もCloudFormation管理外のため無関係）。

1. `template.yaml`の物理リソース名を`task-mcp-server`/`task-api`/`task-agent-job-processor`/`task-manager-users`/`task-manager-webapp`から`taskmanager-mcp`/`taskmanager-api`/`taskmanager-agent-job-processor`/`taskmanager-users`/`taskmanager-webapp`へ変更。
2. 旧スタックのLambda環境変数から`MCP_AUTH_TOKEN`の実値を`aws lambda get-function-configuration`で取得（NoEcho指定はCloudFormation上のパラメータ表示のマスクであり、Lambda環境変数としては読み取れる）。新スタックにも同じ値を渡すことで、Claude Desktop側のBearerトークン設定は変更不要にした。
3. 旧Cognito User Poolのユーザー一覧を`aws cognito-idp list-users`で退避。
4. `aws cloudformation delete-stack --stack-name task-mcp-server`で旧スタックを削除。
5. `sam build --use-container && sam deploy`で新スタック`taskmanager`を作成。
6. 新User Poolに`aws cognito-idp admin-create-user`でユーザーを再作成（Cognito User PoolはCloudFormationリソースとして再作成されるため、UserPool IDごと変わりアカウントの引き継ぎはできない。ユーザーは初回ログイン時にパスワード再設定が必要）。
7. `frontend/src/config.js`を新しいOutputsの値に更新し、Amplify Hostingへpush。

| 項目 | 移行前（`task-mcp-server`、2026-07-27時点） | 移行後（`taskmanager`、2026-08-01） |
|---|---|---|
| メインスタック | `task-mcp-server` | `taskmanager` |
| Web APIエンドポイント | `https://izomsyzy21.execute-api.ap-northeast-1.amazonaws.com` | `https://kvwrqkhd29.execute-api.ap-northeast-1.amazonaws.com` |
| MCP Function URL | `https://okwmgkpa4nihlmbr47nzs6ynj40olrax.lambda-url.ap-northeast-1.on.aws/` | `https://v5ofxfmo2tokwlsukn2io7f27m0stmmr.lambda-url.ap-northeast-1.on.aws/` |
| Cognito User Pool ID | `ap-northeast-1_WgwchFez1` | `ap-northeast-1_HbqJsbWyS` |
| Cognito User Pool Client ID | `5n5nj17i1qensg6fe154kn8k6n` | `3d7teaagegsidv8ke6ih7bf7od` |
| `task-tables`スタック | `CREATE_COMPLETE`（6テーブル、無変更） | 同左（今回の移行では一切触れていない） |
| Amplify Hostingアプリ | `task-manager`（App ID `dlmpnhc94nzt3`） | 同左（再作成不要、`frontend/src/config.js`更新+pushのみ） |

`frontend/src/config.js`は上記「移行後」の値に更新済み。移行後の新スタックに対し、`taskmanager-api`へのLambda直接invoke（`GET /clients`相当のイベント）で既存クライアントデータ（3件）が正しく返ることを確認済み。`taskmanager-mcp`のFunction URLも、認証なしで401・正しいBearerトークンで200を返すことを確認済み（トークンは移行前と同じ値を再利用しているため、Claude Desktop側の設定変更はURLの差し替えのみでよい）。

**外部クライアント側で別途更新が必要な設定**（本リポジトリ外、利用者側の作業）:
- Claude Desktopの`claude_desktop_config.json`のMCPサーバーURLを新しいFunction URLに更新
- claude.aiのカスタムコネクタ登録URLを新しいFunction URLに更新

### 10-4. Amplify Hosting（フロントエンド、変更なし）

`amplify.yml`はリポジトリルート直下に配置する必要がある（`appRoot: frontend`を明示するモノレポ形式）。デプロイフロー: GitHub連携 → `main`ブランチpushごとに自動ビルド・デプロイ。

### 10-5. CI/CD・テスト（変更なし）

GitHub Actions等のCIパイプラインは存在しない。バックエンドは手動`sam deploy`、フロントエンドはAmplify Hostingのpush検知ビルドのみ。テストコード・テストフレームワークともに未導入（12章参照）。開発時の動作確認は`.claude/settings.json`に許可設定されたヘッドレスChromeスクリーンショット取得コマンド（`app.png`/`agents_live.png`等）による目視確認が中心。

---

## 11. 設定・環境変数

`.env`ファイルは存在せず、設定は2箇所に分散している。

**バックエンド（Lambda環境変数、`template.yaml`定義）**:
```
TASKS_TABLE=Tasks, CLIENTS_TABLE=TaskClients, SERIES_TABLE=TaskSeries,
FRAMES_TABLE=TaskFrames, HISTORY_TABLE=TaskHistory, AGENT_JOBS_TABLE=TaskAgentJobs,
CLASSIFICATION_RULES_TABLE=TaskClassificationRules
                                                        （TaskMcpFunction/TaskApiFunction/TaskAgentJobProcessorFunction共通。
                                                         CLASSIFICATION_RULES_TABLEは2026-08-03追加）
MCP_AUTH_TOKEN                                          （TaskMcpFunctionのみ、NoEcho）
AGENT_JOB_PROCESSOR_FUNCTION_NAME                       （TaskApiFunctionのみ、非同期invoke先の関数名）
AGENT_RUNTIME_ARN                                       （TaskAgentJobProcessorFunctionのみ、Bedrock AgentCoreのARN）
```
`task_repository.py`はモジュールロード時に`os.environ[...]`を必須参照するため、未設定だと起動時に即座に失敗する。

**フロントエンド（`frontend/src/config.js`、ビルド時に静的埋め込み）**:
```js
amplifyConfig.Auth.Cognito.userPoolId / userPoolClientId
API_BASE_URL
```
`sam deploy`のOutputs値を手動でこのファイルに反映する運用。環境ごとの分岐（dev/staging/prod）は存在しない（単一環境: `ap-northeast-1`のみを想定）。

---

## 12. 未実装・今後の検討事項（既知の課題の棚卸し）

| # | 課題 | 影響 |
|---|---|---|
| 1 | CORS設定が`AllowOrigins: "*"` | 本番運用前にAmplifyの実ドメインへ限定が必要（セキュリティ） |
| 2 | `skill/SKILL.md` / `skill/bpo-task-manager/SKILL.md`のメール連携記述が未更新 | メール連携機能（`link_email`）は廃止済みだが、Claude Skillの振る舞い指示書には旧仕様の記述が残っている |
| 3 | ポーリング方式（タスク一覧4秒間隔・エージェントジョブ3秒間隔）での更新確認 | DynamoDB Streams + AppSyncまたはWebSocket APIへの置き換えでリアルタイム性向上の余地 |
| 4 | RBAC（権限分離）なし | 利用者が増えた場合、担当者ごとのアクセス制御が必要になる可能性。特にエージェント機能（外部Drive操作を伴う）は誰でも起動できる現状でよいか要検討 |
| 5 | テスト・CI/CD未整備 | 変更の回帰確認が手動デプロイ後の目視・スモークテストのみ |
| 6 | `POST /clients`等の重複時エラー仕様（409）が実運用で十分か未検証 | フロントエンドのエラー表示は簡易的なもの |
| 7 | CSVアップロードが行の生データを`content`に丸ごと詰めるだけ（9-10節） | 列⇔項目のマッピング仕様が未整備。取り込み後の手動整形が前提の暫定実装 |
| 8 | エージェントカタログ中4/5がプレースホルダ（`scout`/`courier`/`auditor`/`pinger`） | `archivist`（分類）以外は未実装。UIのみ先行して用意されている状態 |
| 9 | `AgentsPanel.jsx`のジョブ状態がクライアント側`useState`のみで永続化されない | ページ再読み込みでチケット表示が失われる（裏側の`TaskAgentJobs`レコード自体は1日残る）。ジョブ一覧取得APIがあれば復元可能 |
| 10 | Cognitoユーザーの再作成が必要な運用（10-3節） | User Poolを含むスタックを作り直すたびにアカウントが失われる。利用者が増えるとスタック再作成のコストが上がるため、頻繁な構成変更は避けたい |
| 11 | ~~`task-agent`（旧`receipt-agent`）のAWS再デプロイ未実施~~ → 2026-08-02に解消（4-4-1節） | 新runtime（`task_agent-lAAoax7UkB`）へのデプロイ、IAMポリシー付与、`template.yaml`の`AgentRuntimeArn`更新・taskmanagerスタックの`sam deploy`まで完了・動作確認済み |
| 12 | IAMポリシー（`task-agent/dynamodb-access-policy-clients.json`・`secrets-access-policy-usertoken.json`）がIaC管理外 | AgentCore実行ロールへのアタッチが手動作業のため、ロール再作成時に権限が失われるリスクがある。CDK/Terraform等での管理化、または手順書化が望ましい |
| 13 | `task-agent`の`system_prompt`が「Ikkoh K.K.（`IKK`）向け」に固定 | 複数クライアントを横断的に扱う設計になっていない。他クライアント（`LL`/`MM`等）でも`archivist`を使う場合はプロンプト設計の見直しが必要 |
| 14 | ~~分類機能（タスク履歴の自動分類）が設計のみで未実装~~ → 2026-08-03に解消（4-7節） | `TaskClassificationRules`テーブル（5-8節）・`/classification-axes`系API・`/classify`API・「分類ルール」管理画面・`HistoryPanel.jsx`の「自動判定」ボタンを実装し、`task-tables`（`aws cloudformation deploy`）・`taskmanager`（`sam deploy`）とも本番AWS環境へ反映済み。Web UI上で軸・ルールの作成、自動判定ボタンの動作を確認済み |

---

## 13. 参照した主要ファイル

- `README.md`（2026-07-27時点の記述に留まっており本書更新時点では未反映部分あり） / `template.yaml` / `tables.yaml` / `amplify.yml` / `samconfig.toml`
- `mcp_src/app.py` / `mcp_src/requirements.txt`
- `api_src/app.py`
- `agent_job_src/app.py`
- `layer/python/task_repository.py`
- `task-agent/task_agent.py` / `task-agent/Dockerfile` / `task-agent/.claude/skills/receipt-ocr-filelist/SKILL.md`
- `task-agent/.bedrock_agentcore.yaml`（デプロイ済みruntime情報） / `task-agent/dynamodb-access-policy-clients.json` / `task-agent/secrets-access-policy-usertoken.json`（IAMポリシー） / `task-agent/output.json` / `task-agent/payload.json`（動作確認記録）
- `frontend/src/*.jsx`（`FrameMultiSelect.jsx`含む） / `frontend/src/api.js` / `frontend/src/config.js` / `frontend/package.json`
- `skill/SKILL.md` / `skill/bpo-task-manager/SKILL.md`
- `.claude/settings.json`（開発時の許可コマンド一覧。デプロイ・スクリーンショット確認の運用パターンの参考として参照）
